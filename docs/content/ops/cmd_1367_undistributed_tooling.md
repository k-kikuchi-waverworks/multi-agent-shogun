---
title: "cmd_1367 — 配られておらぬ道具の関所と、その網の狭さ"
description: "whitelist 型 .gitignore で「呼ぶ者は配られるが呼ばれる者は配られぬ」型を毎朝見る gate。併せて、この gate が【見ておらぬ範囲】と、discord 系 2 本の永久非追跡裁定を記す。"
---

# cmd_1367 — 配られておらぬ道具の関所と、その網の狭さ

> ## ★★退役 (2026-07-30 / cmd_1479 / 88aa167)★★
>
> **この文書が説く道具 `scripts/gate_undistributed_tooling.sh` は撤去した。**
> 呼び手であった `scripts/gate_nightly.sh` も同じ commit で撤去した (cron は cmd_1476 で停止済)。
> ⇒ **「配られておらぬ道具」を今 自動で見る者は居ない。** 下の実行例は撃てない。
> ⇒ **文書は残す** — 何を見ていて、何を見ていなかったか (網の狭さ) は、次に建てる者が要る記録ゆえ。
> ⇒ 建て直すかどうかは殿の裁である (計器を建てるのは起票して裁を待つ仕事・CLAUDE.md)。

- **道具**: ~~`scripts/gate_undistributed_tooling.sh`~~ **(撤去済)** — かつては単体実行 / `--selftest` / `gate_nightly.sh` から毎朝
- **三値**: PASS=0 / FAIL=1 / UNDETERMINED=2 (観測できなんだ状態を緑に混ぜぬ)
- **起源**: 足軽五号が「恋の本番を守る唯一の道具が git 追跡外」を発見 (2026-07-26)

---

## 1. 何を検めるか

★追跡下の道具が名指しする repo 内 path のうち、**実在するが HEAD に無い**物★ を出す。

= 呼ぶ者は fresh clone へ配られるが、呼ばれる者は配られぬ。
cmd_1363 で塞いだ「hook 宣言だけ配られ、呼ばれる script が追跡外」の**裏返し**である。

本 repo は whitelist 型 `.gitignore` (`*` で全除外 → 個別許可) ゆえ、
**道具は既定で配られず、名指しを忘れると「手元では動くが clone には無い」形で静かに落ちる**。
同じ穴が cmd_1280 / 1339 / 1354 / 1355 / 1363 と **5 回 別々に開いた**実績があり、
いずれも「見つけた者が 1 行足す」で個別に是正されてきた。
⇒ 1 件ずつ拾う直し方は次に道具が増えた時にまた漏れるゆえ、**族として毎朝見る**。

---

## 2. ★この gate の網は狭い。狭いことを毎 run 名乗る★

> ★狭いことが誤りなのではない。狭いと名乗っておらぬことが誤りである★ (家老・2026-07-26)

軍師一号の検分 (cmd_1367 N3) で、**申告と実力の乖離**が実測された。
ゆえに gate は毎 run、自分の網を印字する (`gate_scope_notice`)。

| | 内容 |
|---|---|
| **見る** | HEAD の `.sh` / `.py` / `.bats` が **平文で** 名指しする `scripts\|lib\|config\|instructions\|templates\|saytask\|agents/` 配下の path |
| **見ぬ ①** | **呼ぶ者が追跡外なら、呼ばれる者も見えぬ** |
| **見ぬ ②** | 呼ぶ者が `.md` / `.ps1` / cron / 人手 / GUI なら見えぬ (走査元は `.sh`/`.py`/`.bats` のみ) |
| **見ぬ ③** | path を変数や連結で組む呼び方は見えぬ (平文一致のみ) |
| **見ぬ ④** | 上記 7 dir の外に在る道具は見えぬ |

⇒ ★本 gate の PASS は「配られておらぬ道具は無い」ではなく
**「この網に掛かる範囲では無い」**である★。

### 2.1 盲点の実測 (推測でなく数字)

- ★cmd_1367 を起こした当の `scripts/gpu_sidecar_stop.sh` を、この gate は名指しできぬ★ =
  HEAD 全 file を走査した結果、**追跡下でその名を含む file は自分自身のみ**であった。
  呼ぶ者が追跡外・人手・runbook であったゆえ、**仮に whitelist から外れても本 gate は黙る**。
- 是正前の状態へ当て直した時、本 gate が surface するのは **5 件**。
  同じ状態を人手で調べた時は **19 件**。

### 2.2 名乗りを試験に据えた理由

`--selftest` の **T11 / T11b** が、**緑でも赤でも scope が出ること**を検める。

> comment に書くだけでは、次に誰かが出力を整理した時に黙って消える。
> ★消えたら赤くなる形にして初めて「名乗り続ける」と言える★。

---

## 3. 数え方 — 何本 (file) と 何件 (参照出現) を分ける

軍師一号の差し戻し N1 / N2 の是正 (2026-07-26)。

**初版の誤り**: 1 参照ごとに 1 行の所見を積んでおった。ゆえに

- 同じ道具が 3 箇所から呼ばれておれば所見が 3 行に膨れ、
- ★「1 本落ちておる」を「6 件」と申告し★ (N2)、
- ★重複行が畳み枠 (`gate_nightly` の `head`) を食って、他の道具を押し出した★ (N1)
  = 実在の道具 3 本を落としても、家老へ届く文面には 2 本しか載らなんだ。

**是正**:

- 所見は ★1 道具 1 行★ へ畳む (呼ぶ者も `sort -u` で畳む)。
- 集計は ★`UNDISTRIBUTED=N本(参照M件)`★ と両方を出す。
- ★検算は参照出現で行う★ = 内訳は参照を分類した物ゆえ、file 数と足しても合わぬ。

**固定した試験**: T9 (3 箇所から呼ばれる 1 本 → `1本(参照3件)` かつ所見 1 行) /
T10 (3 本落ち・うち 1 本は 3 重参照 → **3 本とも名指し**)。

**E2E**: `gate_nightly` が家老へ送る時と同一の畳み方 (`grep | head -4 | awk`) を当て、
3 本とも文面に載ることを実測済。

### 3.1 ★切り詰めたなら「切り詰めた」と言え★ (`gate_nightly.sh` 側)

`gate_nightly.sh` は所見を `head` で畳む箇所が 3 つあり、
いずれも ★入り切らなんだ分を黙って捨てておった★ (残数表示が無い)。
**黙って捨てる要約は、読む者に「これが全部だ」と誤読させる** =
「数字だけ見せて範囲を黙る」の同族ゆえ、3 箇所とも `fold_lines <limit>` へ通した。

```
… ・[UNDISTRIBUTED] scripts/wild_4.sh …・★他 4 行は畳んだ (全 8 行・log を見よ)★・
```

`fold_lines` は 1 行化の従前の作法を保つ
(連結は `awk` — `tr` は多 byte を割る / `": "` は全角へ = inbox YAML 安全)。
**実射で 5 条件を確認**: 上限内 / 超過 (残数が出る) / 空 (何も出さぬ) /
コロン全角化 / 多 byte 非破壊。

> 手当の**順序**についての記録: 当初この file は足軽三号が cmd_1370/1374 で
> 在走中であったため一度 **触らず差し戻した** (RACE-001)。
> 三号の着地 (`fc04b0b`) を実測で確認してから当てておる。

---

## 4. 生成物 allowlist が吸収層にならぬ形

「追跡外で正しい」= fresh clone では**追跡下の生成元が作る**ゆえ repo に無くてよい物。

- 形式は `<path>|<生成元 path>` = ★名前だけの list にはせぬ★。
  生成元が HEAD に在り、**且つその blob が当該 path を名指ししておる**ことを毎回検める。
  生成元から生成が消えれば本 gate は赤くなる (T7 が実証)。
- 件数に上限 (既定 5) を置き、★「黙らせる為に足す」を構造で抑える★ (T8 が実証)。
- ★正直に書く caveat★: 「blob に path 文字列が在る」は「現に生成しておる」より**弱い**。
  生成コードの形 (変数経由の redirect 等) は script ごとに違い、形で判定すると
  見落としと誤検知の両方を生む。**弱いと分かった上で採っておる**。

---

## 5. 規模の層 — この gate が黙る軸

> ★そのgateが黙る規模の軸を選べ★ (cmd_1367 で家老が採った文言)

本 gate が黙る軸は「標本の大きさ」ではなく **走査元と参照の【痩せ】** である:

- `git ls-tree` の glob が変わる / 実行位置が repo 外 → 走査元 0 本 → 常に緑
- 参照抽出の正規表現を壊す → 参照 0 件 → 常に緑

どちらも ★誰にも赤を出さず、ただ何も見なくなる★ 形ゆえ、
下限 (既定 走査元 50 / 参照 100) を割ったら **緑を出さず UNDETERMINED** で止まる (T4/T5)。

---

## 6. ★discord 系 2 本 = 永久に追跡せぬ★ (家老裁定 2026-07-26)

対象: `scripts/discord_channel_inventory.sh` / `scripts/discord_channel_delete.sh`

裁定は `.gitignore` の discord 節にも **inline で** 書いてある。
★次の者が迷うのは docs を読む時ではなく `.gitignore` を編む時★ ゆえ二段で残す。

### 6.1 根拠 (2026-07-26 実測)

| 検めた事 | 実測 |
|---|---|
| literal token の焼き込み | **0 件** (両 script とも `backend/.env` から `DISCORD_TOKEN` を読む) |
| 実 channel / category ID の平文 | ★`delete.sh` に 40 個★ (17〜20 桁 ID の一意出現数) |
| 殿の個人 tree の絶対 path hardcode | `inventory.sh` 3 箇所 / `delete.sh` 2 箇所 |
| 追跡下からの**呼び出し** | **0 件** ⇒ fresh clone は壊れぬ |

★正確に記す★: 名を含む追跡下 file は **1 本ある** —
`skills/discord-ch-state-verify/SKILL.md`。ただしそれは `Do NOT use for` 行での
**「使うな」という名指し**であって呼び出しではない。

> なお、これは §2 の **見ぬ ②** (`.md` は走査せぬ) の実例でもある。
> ★仮にこれが本物の呼び出しであったなら、本 gate はそれを見つけられなんだ★。

### 6.2 判断

- 配らぬ側に倒して**害が無い** (呼ぶ者 0・fresh clone 不壊)。
- 配れば**殿の稼働 guild の構造が 40 個の ID として公開される** (本 repo は OSS fork)。
- ⇒ ★永久非追跡★。
- ★将来 配るなら★: ID 表の外出し + path の env 化が前提。

### 6.3 据置き (追跡せぬまま・軍師一号の実測で実行配線 0 件)

runbook 2 / skills 4 / `setup_user_env.sh` / docs 2 — いずれも既定どおり非追跡のまま。

---

## 7. 走らせ方 — ★撃てない (2026-07-30 / cmd_1479 で撤去済)★

```bash
# ★★下の 2 行は今 撃てない (file が無い)★★ — 当時の形の記録として残す
bash scripts/gate_undistributed_tooling.sh            # 本 repo を検分 (PASS/FAIL/UNDETERMINED)
bash scripts/gate_undistributed_tooling.sh --selftest # 物差し自身の検分 (16 項目)
```

かつては `gate_nightly.sh` が毎朝 06:30 に相乗りで撃ち、非 PASS は家老 inbox へ名指しで警告していた。
★その門も同じ commit (88aa167) で撤去した★ ⇒ **今 撃つ者も、警告を運ぶ者も居ない。**
戻すなら `git show 88aa167^:scripts/gate_undistributed_tooling.sh` に現物が在る。
